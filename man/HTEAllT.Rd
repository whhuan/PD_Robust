% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HTEAllT.R
\name{HTEAllT}
\alias{HTEAllT}
\title{Estimate pooled heterogeneous treatment effects across all times}
\usage{
HTEAllT(
  data,
  ps_fo,
  prin_fo,
  out_fo,
  B,
  conf_level = 0.95,
  max_attempts = NULL,
  verbose = TRUE,
  progress_callback = NULL
)
}
\arguments{
\item{data}{A standardized \code{pd_data} object returned by \code{DataStandard()}.}

\item{ps_fo}{Propensity-score formula.}

\item{prin_fo}{Principal-score formula.}

\item{out_fo}{Outcome-model formula.}

\item{B}{Number of successful subject-level bootstrap replications.}

\item{conf_level}{Confidence level for Wald intervals based on bootstrap SDs.}

\item{max_attempts}{Maximum bootstrap attempts. \code{NULL} uses \code{10 * B}.}

\item{verbose}{Emit bootstrap progress messages.}

\item{progress_callback}{Optional function called with one named progress
list before model fitting, after the point estimate, after every bootstrap
attempt, and when bootstrap processing completes. The list contains
\code{stage}, \code{successful}, \code{requested}, \code{attempts}, \code{max_attempts},
\code{failed_attempts}, \code{complete}, \code{elapsed_seconds}, and \code{updated_at}.
Callback errors warn once and disable further updates without changing the
analysis.}
}
\value{
A \code{pd_hte_pooled} object. \code{analysis_times} gives the complete
baseline-to-cutoff grid, \code{time_effect_estimable} records whether a time
effect was included, and \code{bootstrap_info} records requested and successful
replicates, attempts, completion status, categorized failures, and captured
warning counts, and model diagnostics. Numeric estimates and interval
summaries are rounded to three decimals only after inference; \code{boot_mat}
retains full precision.
}
\description{
\code{HTEAllT()} always uses every actual observed analysis time from the mapped
baseline through the mapped cutoff, inclusive. It does not accept or use
\code{target_time}. The propensity, principal-score, and outcome models are
refitted internally for the point estimate and for every bootstrap sample.
Within one analysis sample, a model fitted to the same rows and formula is
reused only to obtain the two counterfactual treatment predictions.
}
\details{
Repeated finite-prediction separation or convergence messages are
consolidated at the analysis boundary. Model-level details remain available
in \code{model_diagnostics}; bootstrap warnings and their counts are stored in
\code{bootstrap_info}.

If the prepared data contain only one analysis time, the estimator omits the
time-effect term and records that the time effect is not estimable.
}
\examples{
\donttest{
data("BiSample", package = "PDRobust")
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 0, cutoff_time = 2,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"), y_type = "B"
)
pd_dat <- DataStandard(BiSample, map)
fit <- HTEAllT(
  pd_dat,
  A ~ X1 + X2 + X4,
  S ~ X1 + X2 + X4 + A + time,
  Y ~ X1 + X2 + A,
  B = 0
)
fit$summary
}
}
