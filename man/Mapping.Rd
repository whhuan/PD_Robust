% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mapping.R
\name{Mapping}
\alias{Mapping}
\title{Define the PDRobust data mapping}
\usage{
Mapping(
  id,
  time,
  treatment,
  survival,
  outcome,
  baseline_time,
  cutoff_time,
  covariates,
  interest_vars,
  y_type
)
}
\arguments{
\item{id}{Character scalar naming the subject ID column.}

\item{time}{Character scalar naming the analysis time column.}

\item{treatment}{Character scalar naming the treatment column.}

\item{survival}{Character scalar naming the survival/intermediate status column.}

\item{outcome}{Character scalar naming the outcome column.}

\item{baseline_time}{One finite numeric baseline time in the raw time scale.}

\item{cutoff_time}{One finite numeric cutoff time in the raw time scale.}

\item{covariates}{Character vector naming every non-structural variable used
in any prediction-model formula.}

\item{interest_vars}{Character vector naming effect modifiers or profiling
variables. Every entry must also occur in \code{covariates}.}

\item{y_type}{Outcome type code: \code{"C"} for continuous or \code{"B"} for binary.}
}
\value{
A \code{pd_mapping} object.
}
\description{
Creates the single source of truth for structural columns, baseline and
cutoff times, prediction-model covariates, effect modifiers, and outcome type.
\code{target_time} is deliberately not stored in the mapping; it is an argument
of \code{HTESepT()} only.
All ten arguments are required; no structural role or analysis setting is
inferred or defaulted.
}
\examples{
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 3,
  cutoff_time = 9,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"),
  y_type = "C"
)
map
}
