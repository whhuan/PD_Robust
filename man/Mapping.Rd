% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mapping.R
\name{Mapping}
\alias{Mapping}
\title{Identify variables and analysis times for PDRobust}
\usage{
Mapping(
  id,
  time,
  treatment,
  survival,
  outcome,
  baseline_time,
  cutoff_time,
  covariates,
  interest_vars,
  y_type
)
}
\arguments{
\item{id}{A single character string naming the subject ID column.}

\item{time}{A single character string naming the time column.}

\item{treatment}{A single character string naming the treatment column. For causal
estimation, code the survival-favorable arm as \code{1} and the other arm as
\code{0}; see \link{PDRobust-package} for the convention and assumptions. The
function does not determine which arm is survival-favorable.}

\item{survival}{A single character string naming the column that records
survival or another intermediate status.}

\item{outcome}{A single character string naming the outcome column.}

\item{baseline_time}{A single finite number giving the baseline time in the
original time scale.}

\item{cutoff_time}{The time point, on the original time scale, at which the
always-survivor principal stratum is defined.}

\item{covariates}{A character vector naming all variables used as predictors
in the propensity score, principal score, or outcome mean models.}

\item{interest_vars}{A character vector specifying the names of variables
used to evaluate heterogeneous treatment effects. Each variable must also
be included in \code{covariates}.}

\item{y_type}{The outcome type: \code{"C"} for continuous or \code{"B"} for binary.}
}
\value{
A \code{pd_mapping} object that can be supplied to \code{DataCheck()} and
\code{DataStandard()}.
}
\description{
Records which columns contain the subject ID, time, treatment, survival,
outcome, and covariates, together with the analysis time range and outcome
type. Other package functions use this information to interpret the data
consistently.
}
\details{
All ten arguments are required. \code{target_time} is specified separately when
calling \code{HTESepT()} because it selects time points for that analysis rather
than describing the data.
}
\examples{
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 3,
  cutoff_time = 9,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"),
  y_type = "C"
)
map
}
