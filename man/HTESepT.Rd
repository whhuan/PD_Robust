% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HTESepT.R
\name{HTESepT}
\alias{HTESepT}
\title{Separate estimation of heterogeneous treatment effects by time point}
\usage{
HTESepT(
  data,
  ps_fo,
  prin_fo,
  out_fo,
  target_time,
  B,
  conf_level = 0.95,
  max_attempts = NULL,
  verbose = TRUE,
  progress_callback = NULL
)
}
\arguments{
\item{data}{Data prepared by \code{DataStandard()}.}

\item{ps_fo}{propensity score model formula}

\item{prin_fo}{principal score model formula}

\item{out_fo}{outcome mean model formula}

\item{target_time}{A non-empty numeric vector containing timepoints of
interest in standardized form. Baseline is allowed.}

\item{B}{Number of bootstrap replications. Use \code{0} for point estimates only.}

\item{conf_level}{The confidence level for Wald intervals calculated from
bootstrap standard errors.}

\item{max_attempts}{The maximum number of resampling attempts allowed to
obtain \code{B} successful bootstrap replications. Defaults to \verb{10B}.
Resampling stops once \code{B} successful replications are obtained or when the
maximum number of attempts is reached, whichever occurs first. Thus,
fewer than \code{B} successful replications may be returned if the maximum
number of attempts is reached.}

\item{verbose}{If \code{TRUE}, print bootstrap progress messages.}

\item{progress_callback}{An optional function for receiving bootstrap
progress updates. It is called before model fitting, after the point
estimate, after every bootstrap attempt, and when bootstrapping finishes.
Each update is a named list containing
\code{stage}, \code{successful}, \code{requested}, \code{attempts}, \code{max_attempts},
\code{failed_attempts}, \code{complete}, \code{elapsed_seconds}, and \code{updated_at}.
If the callback produces an error, the function warns once and stops
sending updates; the statistical analysis continues.}
}
\value{
A \code{pd_hte_timevarying} object containing the estimate for each
requested time, confidence intervals when \code{B > 0}, model-checking
information, and a summary of successful and failed bootstrap attempts.
Displayed estimates are rounded to three decimal places; \code{boot_mat} stores
the unrounded bootstrap coefficients.
}
\description{
Performs separate analyses of heterogeneous treatment effects at each
selected time point.
}
\details{
\code{HTESepT()} combines predictions from the propensity score, principal score,
and outcome mean models to form the data used for effect estimation. It fits
a separate treatment-effect model at each selected time and, when requested,
uses bootstrap resampling to calculate confidence intervals.
}
\section{Numerical safeguards}{

Propensity scores are limited to \verb{[0.01, 0.99]}. Their product with estimated
survival probabilities under treatment \code{1} is limited to \verb{[0.005, 0.995]}
when effects are estimated. These limits prevent division by probabilities
very close to zero, but they can affect the estimates and do not demonstrate
adequate overlap or validate the causal assumptions. Review the returned
model information, especially when few subjects remain at risk.
}

\section{Treatment coding}{

The implemented estimator uses treatment \code{1} as the survival-favorable arm:
potential survival satisfies \eqn{S^1 \ge S^0} at cutoff. Its always-survivor
principal score is therefore the survival probability under treatment \code{0}.
If the survival-favorable arm is coded as \code{0} in the raw data,
recode the raw treatment as \code{1 - A} before mapping and
standardizing. To report the original contrast, negate the package estimate
and transform an interval \verb{[lower, upper]} to \verb{[-upper, -lower]}.
\code{Mapping()} does not infer or reverse treatment coding.
}

\examples{
\donttest{
data("BiSample", package = "PDRobust")
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 0, cutoff_time = 2,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"), y_type = "B"
)
pd_dat <- DataStandard(BiSample, map)
fit <- HTESepT(
  pd_dat,
  A ~ X1 + X2 + X4,
  S ~ X1 + X2 + X4 + A + time,
  Y ~ X1 + X2 + A,
  target_time = c(0, 2), B = 0
)
fit$summary
}
}
\seealso{
\link{PDRobust-package}, \link{pd_methods}
}
