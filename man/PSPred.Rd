% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PSPred.R
\name{PSPred}
\alias{PSPred}
\title{Estimate propensity scores}
\usage{
PSPred(ps_fo, fit_dat, pred_dat, mapping, ...)
}
\arguments{
\item{ps_fo}{Propensity-score formula.}

\item{fit_dat}{Data used to fit the model.}

\item{pred_dat}{Data on which to predict.}

\item{mapping}{A \code{pd_mapping} object. It supplies column names and
\code{baseline_time} only; it never supplies data.}

\item{...}{Additional arguments passed to \code{stats::glm()}.}
}
\value{
A numeric vector of class \code{pd_prediction} with length
\code{nrow(pred_dat)}, rounded to three decimal places after prediction.
}
\description{
Fits a logistic propensity-score model on baseline observations from
\code{fit_dat} and predicts on every row of \code{pred_dat}. The model is refitted on
every call; no fitted object or cache is retained.
}
\examples{
data("BiSample", package = "PDRobust")
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 0, cutoff_time = 2,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"), y_type = "B"
)
pd_dat <- DataStandard(BiSample, map)
ps <- PSPred(A ~ X1 + X2 + X4, pd_dat, pd_dat, map)
head(ps)
}
