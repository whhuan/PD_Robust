% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PSPred.R
\name{PSPred}
\alias{PSPred}
\title{Estimate propensity scores}
\usage{
PSPred(ps_fo, fit_dat, pred_dat, mapping, ...)
}
\arguments{
\item{ps_fo}{propensity score model formula}

\item{fit_dat}{A data frame containing the baseline observations used to fit
the model.}

\item{pred_dat}{A data frame containing the observations for which propensity
scores are requested.}

\item{mapping}{A \code{pd_mapping} object that identifies the treatment and time
columns and the baseline time.}

\item{...}{Additional arguments passed to \code{stats::glm()}.}
}
\value{
A numeric vector of propensity scores, one for each row of
\code{pred_dat}, rounded to three decimal places.
}
\description{
Fits a logistic model using baseline observations from \code{fit_dat} and returns
each row's estimated probability of receiving treatment \code{1} in \code{pred_dat}.
The model is fitted again each time the function is called.
}
\examples{
data("BiSample", package = "PDRobust")
map <- Mapping(
  id = "id", time = "time", treatment = "A",
  survival = "S", outcome = "Y",
  baseline_time = 0, cutoff_time = 2,
  covariates = c("X1", "X2", "X4"),
  interest_vars = c("X1", "X2"), y_type = "B"
)
pd_dat <- DataStandard(BiSample, map)
ps <- PSPred(A ~ X1 + X2 + X4, pd_dat, pd_dat, map)
head(ps)
}
